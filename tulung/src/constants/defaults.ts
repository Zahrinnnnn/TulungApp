/**
 * Default application constants
 */

/**
 * Default daily budget in USD
 * Used when user hasn't set a budget yet
 */
export const DEFAULT_DAILY_BUDGET = 50.00;

/**
 * Default currency code
 */
export const DEFAULT_CURRENCY = 'USD';

/**
 * Default timezone
 */
export const DEFAULT_TIMEZONE = 'UTC';

/**
 * Free tier scan quota per month
 */
export const FREE_TIER_SCAN_LIMIT = 10;

/**
 * App version
 */
export const APP_VERSION = '1.0.0';
